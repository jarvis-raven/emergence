"""Emergence memory system — artifact-based continuity for agent selfhood.

This package provides:
- Flush prompt rendering for OpenClaw compaction
- Consolidation engine for session processing
- Nightly build for identity review
- Self-history snapshots for versioned identity
- Memory conventions and file structure

Example usage:
    from core.memory.flush_prompt import render_flush_prompt
    from core.memory.consolidation import run_consolidation
    from core.memory.self_history import create_snapshot
    from core.memory.nightly_build import run_nightly_build
"""
